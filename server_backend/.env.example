# Database connection string for MySQL using Prisma.
# Replace placeholder values or ensure the deployment agent maps these correctly.
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://username:password@localhost:3306/furniro"

# JWT secret for signing tokens (backend auth flows will use this).
JWT_SECRET="replace_with_a_secure_random_secret"

# Node environment (development|production|test)
NODE_ENV="development"

# Server host/port can be configured by the runtime environment if needed
# HOST="0.0.0.0"
# PORT="3001"
